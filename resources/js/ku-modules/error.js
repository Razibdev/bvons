/*
import notify from '../../public/js/plugins/bootstrap-notify/bootstrap-notify.min';

import Helpers from "./codebase/modules/helpers";
export default function showError(message) {

    let msg = message == undefined || message.length == 0 ? ["Some Error occurred"] : message;
    let msgView = '<ul>';
    for(let i = 0; i < msg.length; i++) {
        msgView += '<li>' + msg[i] + '</li>';
    }
    msgView += '</ul>';
    Helpers.notify({
        align: 'right',                                 // 'right', 'left', 'center'
        from: 'bottom',                                 // 'top', 'bottom'
        type: 'danger',                                 // 'info', 'success', 'warning', 'danger'
        icon: 'fa fa-exclamation-triangle mr-5',        // Icon class
        message: msgView
    });
}


*/
